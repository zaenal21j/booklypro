<div class="flex items-center gap-3 <%= active ? "text-ink" : "text-muted" %>"><span class="grid size-8 place-items-center rounded-full <%= active ? "bg-ink text-white" : "border border-line bg-white" %> text-xs font-bold"><%= number %></span><span class="hidden text-sm font-semibold sm:inline"><%= label %></span></div>
