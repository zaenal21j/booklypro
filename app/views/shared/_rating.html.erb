<div class="flex items-center gap-1.5 text-sm" aria-label="Rated <%= rating %> out of 5 from <%= reviews %> reviews">
  <span class="text-gold">★</span><strong><%= rating %></strong><span class="text-muted">(<%= reviews %>)</span>
</div>
